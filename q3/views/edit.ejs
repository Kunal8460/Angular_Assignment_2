<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>Edit</title>
</head>

<body>
    <div class="container p-5">
        <form method="post" action="/update/<%= getStudent._id %>">
            <h3>Add Student</h3>
            <div class="col-sm-6 pb-5">
                <label for="rollno" class="label pt-3">Roll number</label>
                <div class="pt-2">
                    <input type="number" class="form-control" value="<%= getStudent.rollno %>" name="rollno"
                        id="rollno">
                </div>
                <label for="name" class="label pt-3">Name</label>
                <div class="pt-2">
                    <input type="text" class="form-control" value="<%= getStudent.name %>" name="name" id="name">
                </div>
                <label for="gender" class="label pt-3">Gender</label>
                <div class="pt-2">
                    <select name="gender" class="form-control" id="gender">
                        <option value="">Select Gender</option>
                        <% if(getStudent.gender=="Male" ){ %>
                            <option value="Male" selected>Male</option>
                            <option value="Female">Female</option>
                            <% }else{ %>
                                <option value="Male">Male</option>
                                <option value="Female" selected>Female</option>
                                <% } %>
                    </select>
                </div>
                <label for="email" class="label pt-3">Email</label>
                <div class="pt-2">
                    <input type="email" class="form-control" value="<%= getStudent.email %>" name="email" id="email">
                </div>
                <label for="contactno" class="label pt-3">Contact No. </label>
                <div class="pt-2">
                    <input type="text" class="form-control" value="<%= getStudent.contact %>" name="contactno"
                        id="contactno">
                </div>
                <label for="address" class="label pt-3">Address</label>
                <div class="pt-2">
                    <input type="text" class="form-control" value="<%= getStudent.address %>" name="address"
                        id="address">
                </div>
                <div class="pt-2">
                    <input type="submit" value="Login" id="login" class="btn btn-outline-primary">
                </div>
            </div>
        </form>
    </div>
</body>

</html>